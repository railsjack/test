name: Build docs for Bills Microservice

on:
  push:
    branches:
      - main
# env:
#   ENVIRONMENT: dev

jobs:
  manage:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Test
        run: |
          echo "here"
          echo "${{ env.ENV_CONTEXT_VAR }}"
          echo "${{ env.TEST_VAR }}"
          echo "${{ env }}" | jq
